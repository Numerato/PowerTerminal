<Window x:Class="PowerTerminal.Views.GoToLineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="280" SizeToContent="Height"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Border BorderBrush="#777777"
            BorderThickness="1"
            CornerRadius="8"
            Background="{StaticResource BackgroundBrush}"
            ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource TitleBarBrush}"
                    BorderBrush="#3b3b3c"
                    BorderThickness="0,0,0,1"
                    CornerRadius="7,7,0,0">
                <Grid MouseLeftButtonDown="TitleBar_MouseDown">
                    <TextBlock Text="Go To Line"
                               FontWeight="SemiBold" FontSize="12"
                               Foreground="{StaticResource ForegroundDimBrush}"
                               VerticalAlignment="Center" Margin="14,0,0,0"/>
                    <Button Style="{StaticResource DialogCloseButton}"
                            HorizontalAlignment="Right"
                            Click="Cancel_Click">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="16,14,16,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Line number:" FontSize="13" Margin="0,0,0,6"/>
                <TextBox Grid.Row="1" x:Name="LineInput" Margin="0,0,0,14"
                         KeyDown="LineInput_KeyDown"/>
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Go" Style="{StaticResource AccentButton}"
                            Click="OK_Click" Margin="0,0,8,0" MinWidth="60" IsDefault="True"/>
                    <Button Content="Cancel" Style="{StaticResource FlatButton}"
                            Click="Cancel_Click" MinWidth="60" IsCancel="True"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
