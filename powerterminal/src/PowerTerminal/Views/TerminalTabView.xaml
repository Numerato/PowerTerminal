<UserControl x:Class="PowerTerminal.Views.TerminalTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:Terminal.Controls;assembly=Terminal.Control"
             xmlns:local="clr-namespace:PowerTerminal.Views"
             Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <ctrl:TerminalControl x:Name="Terminal"
                              Grid.Column="0"
                              FontFamily="{Binding Theme.FontFamily}"
                              FontSize="{Binding Theme.FontSize}"
                              BackgroundColor="{StaticResource BackgroundColor}"
                              Margin="{Binding TerminalPadding}"
                              ScrollbackChanged="Terminal_ScrollbackChanged"/>

        <ScrollBar x:Name="TerminalScrollBar"
                   Grid.Column="1"
                   Orientation="Vertical"
                   Minimum="0"
                   SmallChange="1"
                   Visibility="Collapsed"
                   Style="{StaticResource TerminalScrollBar}"
                   Scroll="TerminalScrollBar_Scroll"/>

        <!-- PowerEdit editor — shown in place of the terminal when a file is open -->
        <local:PowerEditView x:Name="PowerEditor"
                             Grid.Column="0"
                             Visibility="Collapsed"/>
    </Grid>
</UserControl>
